# Official Sandkit API: 0.5.5 → 0.5.6

Source: [`0.5.5.md`](./0.5.5.md) → [`0.5.6.md`](./0.5.6.md)

Net: additions only. No removals of existing APIs.

---

## Summary

| Area | Change |
| --- | --- |
| Config overrides | Documented defaults for five pet configs (`lumling`, `resinWeaver`, `shinelet`, `voidgrazer`, `voltblub`) |
| Hooks | `resource:collection:prepare` feedback gains `"reduced"` |
| Events | New `building:removing` (fires before `building:removed`) |
| Terrains (main + worker) | New `meltAtCell(cellX, cellY)` |
| Hooks (worker) | New intercept `fire:terrain:burn` |

---

## Types package

These are declared in `@sandustry-modding/types` (see `CHANGELOG.md` Unreleased).
